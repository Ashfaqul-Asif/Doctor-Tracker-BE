.env
.env.*
!.env.example

node_modules/
dist/
coverage/

# Generated fresh on every Vercel deploy by `npm run vercel-build` (see
# scripts/build-vercel-function.mjs) — a build artifact, not source.
api/

*.log
npm-debug.log*
.DS_Store
jar.txt
.vitest/

# Claude Code's local state — machine-specific, never shared.
.claude/
CLAUDE.md

# OS / editor cruft
Thumbs.db
.vscode/
.idea/
*.swp
